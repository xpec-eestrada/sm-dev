var config = {
    paths: {
        "slick":        'https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.6.0/slick.min'
    },
    shim: {
        'slick': {
            deps: ['jquery']
        }
    }
};